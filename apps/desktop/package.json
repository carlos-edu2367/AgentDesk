{
  "name": "agentdesk-desktop",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently \"npm run wait-frontend\" \"electron .\"",
    "wait-frontend": "wait-on http://localhost:5173 && echo Frontend ready"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.0.0",
    "electron": "^31.0.0",
    "wait-on": "^7.2.0"
  }
}
